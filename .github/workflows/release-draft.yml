name: Draft Release

on:
  push:
    tags:
      - 'v*'

jobs:
  draft:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Draft release placeholder
        run: echo "Create release notes and store assets here."
